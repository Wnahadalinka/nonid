let config = {
    debugMode: true
};

if (config.debugMode) {
    console.log("Debug mode is enabled.");
    // Additional debug-specific code goes here...
} else {
    console.log("Debug mode is disabled.");
}
